info:
  name: Get guide by id
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/guides/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
